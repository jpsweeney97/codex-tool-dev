# must pass: quoted and template-looking prose stays inert data
schema: deliberate-envelope/v1
stage: generate
status: completed
artifacts:
  field:
    - wording: "Option A — file sync over Syncthing"
      provenance: user-seed
    - wording: "Option B — hosted wiki"
      provenance: generated
  fixed-points-line: |
    Untouched fixed points: the two-person team size and the "must run offline"
    constraint were never varied — {{ template-looking braces }}, $(subshell-looking),
    and !bang prose all stay inert data inside this block scalar.
retrievals: none
encounters: none
pins: none
model: unknown
