[project]
name = "ticket-plugin"
version = "1.4.0"
description = "Repo-local ticket management plugin for Codex"
requires-python = ">=3.11"
dependencies = ["pyyaml>=6.0"]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
