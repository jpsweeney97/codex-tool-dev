# Skill Template

Copy this directory to `.codex/skills/<new-skill-name>/` and replace placeholders in `SKILL.md`.

Required:

- Keep the required headings unchanged.
- Add at least one happy-path and one edge-case example.
- Add verification steps that are executable when possible.

